[pytest]
testpaths = unit integration
python_files = test_*.py *_test.py
pythonpath = ../../backend

# Show short summary for failures and errors
addopts = -ra -q --import-mode=importlib
asyncio_mode = auto
